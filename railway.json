{
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx tsx apps/api/src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}