{
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "npx prisma generate --schema=apps/api/prisma/schema.prisma && npx tsx apps/api/src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}